[logger]
    mode = "debug"
    timestamp = "time"

[authorization]
    [[authorization.users]]
        username = "username"
        password = "password"

[api]
    enable = true
    auth = false
    env = "development"
    [api.parsing]
        enable = true

[store]
    keys = ["keys.json"]
    tmp = "tmp"
    [[store.sources]]
        keys = ["key.json"]
        namespace = "configs"
        type = "folder"
        include_regexp = "\\.(json|yml|toml)$"
        exclude_regexp = "(.*\\/\\.git.*|.*\\\\.git.*)"
        [store.sources.git]
            interval = 5
            branch = "tests/unit/folder_store"